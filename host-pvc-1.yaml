apiVersion: v1 
kind: PersistentVolumeClaim
metadata:
  name: host-pvc-1
spec:
  volumeName: host-pv-1
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources: 
    requests:
      storage: 1Gi
